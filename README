This is a Texas Holdem App

| Node.js | Socket.io | HTML Canvas | React |

** Installation **
  
  For now, the only piece working is the ability to create a lobby, and join a lobby, based on a unique Lobby Code

1. Clone the repo
2. Run 'npm install' in the terminal to install the packages in package.json
3. Run 'npm start' to start the server
4. Go to 'localhost:5000' in your browser
